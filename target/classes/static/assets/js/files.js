console.log('files ...')

hide = function (selector) {
    $(selector).hide()
}

show = function (selector) {
    $(selector).show()
}

triggerFileInput = function (selector) {
    $(selector).click()
}